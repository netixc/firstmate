# Project-memory verification

`bin/fm-ensure-agents-md.sh` owns Firstmate's canonical `AGENTS.md` project-memory file.
`tests/fm-ensure-agents-md.test.sh` proves skeleton creation, idempotent maintenance-section injection, regular-file safety, and case-variant refusal.
The helper does not create compatibility aliases or modify a separate memory convention.

`AGENTS.md` section 6 and the `stow` skill own knowledge routing.
